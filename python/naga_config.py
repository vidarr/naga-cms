import os.path
import sys

PAGE_ROOT      = '/seite'
PATH_SEPARATOR = '/'
CONTENT_DIR    = 'content'
FORMATTING_DIR = 'formatting'
IMAGE_DIR      = 'images'
RSS_ICON_PATH  = PAGE_ROOT + PATH_SEPARATOR + IMAGE_DIR   + PATH_SEPARATOR + 'feed_icon.svg'
RSS_FEED_PATH  = PAGE_ROOT + PATH_SEPARATOR + CONTENT_DIR + PATH_SEPARATOR + 'news.xml'

